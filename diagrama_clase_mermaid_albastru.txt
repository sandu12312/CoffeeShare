DIAGRAMA DE CLASE COFFEESHARE - BOXURI ALBASTRE
==============================================

Codul Mermaid complet pentru diagrama de clase cu stilizare albastră:

```mermaid
%%{init: {'theme':'base', 'themeVariables': { 'primaryColor': '#C5E1FF', 'primaryTextColor': '#000000', 'primaryBorderColor': '#4A90E2', 'lineColor': '#333333', 'secondaryColor': '#E8F4FD', 'tertiaryColor': '#FFFFFF', 'background': '#FFFFFF', 'mainBkg': '#C5E1FF', 'secondBkg': '#E8F4FD', 'tertiaryColor': '#FFFFFF'}}}%%
classDiagram
    direction TB
    
    class User {
        -String uid
        -String email
        -String displayName
        -String phoneNumber
        -String role
        -Date createdAt
        -Date lastLogin
        -Number loyaltyPoints
        -Boolean isActive
        +register() Boolean
        +login() Boolean
        +updateProfile() Boolean
        +changePassword() Boolean
        +getLoyaltyPoints() Number
        +addLoyaltyPoints() Boolean
    }

    class Subscription {
        -String subscriptionId
        -String userId
        -String type
        -Number totalBeans
        -Number usedBeans
        -Number remainingBeans
        -Date startDate
        -Date endDate
        -Boolean isActive
        +createSubscription() Boolean
        +renewSubscription() Boolean
        +useBeans() Boolean
        +getRemainingBeans() Number
        +cancelSubscription() Boolean
    }

    class QRCode {
        -String qrId
        -String userId
        -String qrValue
        -Date generatedAt
        -Date expiresAt
        -Boolean isUsed
        -String locationId
        +generateQR() String
        +validateQR() Boolean
        +markAsUsed() Boolean
        +isExpired() Boolean
    }

    class Transaction {
        -String transactionId
        -String userId
        -String qrCodeId
        -Number beansUsed
        -Date scannedAt
        -String locationId
        -String status
        +createTransaction() Boolean
        +processTransaction() Boolean
        +updateStatus() Boolean
    }

    class CoffeeShop {
        -String shopId
        -String name
        -String address
        -String phoneNumber
        -String email
        -Boolean isActive
        -String ownerId
        +registerShop() Boolean
        +updateShopInfo() Boolean
        +validateQRCode() Boolean
    }

    class Admin {
        -String adminId
        -String userId
        -String permissions
        -Date assignedAt
        +manageUsers() Boolean
        +viewAnalytics() Object
        +configureSystem() Boolean
    }

    class Analytics {
        -String analyticsId
        -String userId
        -Date date
        -Number dailyScans
        -Number weeklyScans
        +trackUserActivity() Boolean
        +generateWeeklyStats() Object
    }

    class SecurityService {
        -String deviceId
        -String userId
        -Boolean isDeviceSecure
        +checkDeviceSecurity() Boolean
        +validateSSLPinning() Boolean
        +detectJailbreak() Boolean
    }

    class PaymentService {
        -String paymentId
        -String userId
        -Number amount
        -String currency
        +processPayment() Boolean
        +refundPayment() Boolean
    }

    User *-- Subscription
    User *-- QRCode
    Subscription *-- Transaction
    
    User o-- SecurityService
    
    User -- Analytics
    QRCode -- Transaction
    CoffeeShop -- Transaction
    CoffeeShop -- User
    
    User <|-- Admin
    
    Subscription ..> PaymentService
    QRCode ..> SecurityService
    Transaction ..> Analytics
```

INSTRUCȚIUNI DE UTILIZARE:
==========================

1. Copiază ÎNTREGUL cod de mai sus (inclusiv linia %%{init:...}%%)
2. Deschide Mermaid Live Editor: https://mermaid.live/
3. Lipește codul în editor
4. Boxurile vor fi albastre ca în Visual Paradigm
5. Exportă ca PNG/SVG pentru Word

SIMBOLURI UML:
==============

*-- = Composition (diamant plin)
o-- = Aggregation (diamant gol)
-- = Association (linie simplă)
<|-- = Inheritance (triunghi)
..> = Dependency (linie punctată)

PENTRU LUCRAREA DE LICENȚĂ:
===========================

Inserează diagrama la punctul 4.3 cu titlul:
"Figura 4.3: Diagrama de clase CoffeeShare" 